import { CloseDataTableDirective } from './close-data-table.directive';

describe('CloseDataTableDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseDataTableDirective();
    expect(directive).toBeTruthy();
  });
});
